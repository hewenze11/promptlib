# Harbor imagePullSecret（在 promptlib namespace 下）
# 部署前执行：
# kubectl create secret docker-registry harbor-secret \
#   --docker-server=47.239.171.228 \
#   --docker-username=admin \
#   --docker-password='HeWenZe!@#456' \
#   -n promptlib
